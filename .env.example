# Copy to `.env.local` and adjust as needed.

# API mode: `mock` (default) or `real`
VITE_API_MODE=mock

# Base URL for backend API when using `real` mode.
# Leave empty to use same-origin.
VITE_API_BASE_URL=

